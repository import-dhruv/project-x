{
  "name": "employee-intelligence-backend",
  "version": "0.1.0",
  "description": "Backend API and analytics engines for Employee Intelligence MVP",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "build": "prisma generate",
    "lint": "eslint .",
    "check": "node --check src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:migrate": "prisma migrate deploy",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "cors": "^2.8.5",
    "csv-parse": "^5.6.0",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "node-cron": "^4.2.1",
    "puppeteer": "^24.20.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "prisma": "^6.16.2"
  },
  "engines": {
    "node": ">=20"
  }
}
